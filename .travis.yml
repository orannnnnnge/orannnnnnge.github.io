language:
  node_js

node_js:
  stable

install:
  - npm install

script:
  - hexo clean
  - hexo generate

after_script:
  - cd ./public
  - git init
  - git config user.name "orannnnnnge"
  - git config user.email "shenxiaohu1996@gmail.com"
  - git add .
  - git commit -m "update"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master

branches:
  only:
    - source

env:
  global:
    - GH_REF: github.com/orannnnnnge/orannnnnnge.github.io.git
